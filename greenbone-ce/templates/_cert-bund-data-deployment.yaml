apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose-22.4.yml
    kompose.version: 1.30.0 (9d8dcb518)
  creationTimestamp: null
  labels:
    io.kompose.service: cert-bund-data
    {{- include "greenbone-ce.labels" . | nindent 4 }}
  name: cert-bund-data
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: cert-bund-data
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose-22.4.yml
        kompose.version: 1.30.0 (9d8dcb518)
      creationTimestamp: null
      labels:
        io.kompose.network/greenbone-helm-chart-default: "true"
        io.kompose.service: cert-bund-data
    spec:
      containers:
        - image: greenbone/cert-bund-data
          name: cert-bund-data
          resources: {}
          volumeMounts:
            - mountPath: /mnt
              name: cert-data-vol
      restartPolicy: Always
      volumes:
        - name: cert-data-vol
          persistentVolumeClaim:
            claimName: cert-data-vol

